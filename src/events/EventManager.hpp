#ifndef LONG_REST_EVENT_MANAGER_HEADER
#define LONG_REST_EVENT_MANAGER_HEADER

namespace LongREST 
{
	namespace events
	{
		// Define event callbacks
		// TODO: Setup callbacks...
		typedef void (*OnResponse)();
		typedef void (*OnRequest)();
	}
}

#endif